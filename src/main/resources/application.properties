spring.application.name=mirakuru-tracker
server.port=8080

# Surface domain failures as clean JSON (handled by GlobalExceptionHandler)
# instead of the default whitelabel page, and never leak stack traces.
server.error.include-stacktrace=never
server.error.include-message=always
